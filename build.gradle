plugins {
  id "org.sonarqube" version "3.1.1"
}

sonarqube {
  properties {
    property "sonar.projectKey", "gautampachnanda_12factor-from-scratch"
    property "sonar.organization", "gautampachnanda"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
